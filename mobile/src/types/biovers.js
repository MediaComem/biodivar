export default class Biovers {
    constructor(id, name, description, location, is_public, is_editable) {
        this.id = id;
        this.name = name;
        this.description = description;
        this.location = location;
        this.is_public = is_public;
        this.is_editable = is_editable;
    }
}